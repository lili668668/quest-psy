<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>
<body>
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            <div class="page-header text-center">
              <h1>歡迎來到大平台的後台</h1>
            </div>
            <div>
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>學號</th>
                            <th>班級</th>
                            <th>姓名</th>
                            <th>連絡電話</th>
                            <th>信箱</th>
                            <th>是否是從眾實驗者</th>
                            <th>兌換的口味</th>
                            <th>在兌換時的人數</th>
                            <th>花了多久時間(分：秒：千毫秒)提交對換</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var i=0; i < data.length; i++) { %>
                       <tr>
                            <td><%= data[i].sid %></td>
                            <td><%= data[i].class %></td>
                             <td><%= data[i].name %></td>
                             <td><%= data[i].phone %></td>
                             <td><%= data[i].email %></td>
                             <td><%= data[i].type %></td>
                             <td><%= data[i].taste %></td>
                             <td><%= data[i].peonum %></td>
                             <td><%= data[i].time %></td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
            <div class="text-center">
                <a href="/logout" class="btn btn-success">登出</a>
            </div>
        </div>
        <div class="col-md-1"></div>
    </div>
</body>
</html>
